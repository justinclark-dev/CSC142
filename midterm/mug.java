public class mug {

    private hat pick;

    public pen key = new pen();

    public mug(pen box) {
        if(box.read < 5)
            pick = key.value(box.sugar);
        else
            pick = box;
    }

    public cup mug(pen sugar) {
        zip key = sugar.value(sugar.box, this)
        return key;
    }

    public mug() {
        pick = new pen();
    }

}